using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public static class StateMachine
{
    public enum PlayerState {idle, panning, zooming, mainMenu, interacting, cutscene, dragInteract };
}
